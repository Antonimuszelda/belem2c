# ========================================
# VARIÁVEIS DE AMBIENTE - BACKEND
# ========================================
# Copie este arquivo para .env e preencha com seus valores reais
# NUNCA commite o arquivo .env com credenciais reais!

# 1. OBRIGATÓRIO - Google Earth Engine
D_DO_PROJETO_GEE=seu-projeto-gee-id
GOOGLE_APPLICATION_CREDENTIALS_JSON_BASE64=suas-credenciais-em-base64

# 2. OBRIGATÓRIO - Google AI (Agente Sacy)
GOOGLE_API_KEY=sua-chave-google-api

# 3. OPCIONAL - Frontend URL (para CORS)
FRONTEND_URL=http://localhost:5173

# ========================================
# DEPLOY NO RENDER.COM
# ========================================
# Build Command: pip install -r requirements.txt
# Start Command: gunicorn app.main:app -w 2 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT --timeout 120
#
# Environment Variables (copie do seu .env local):
# - D_DO_PROJETO_GEE
# - GOOGLE_APPLICATION_CREDENTIALS_JSON_BASE64
# - GOOGLE_API_KEY
# - FRONTEND_URL (ajustar para URL do Vercel após deploy)
