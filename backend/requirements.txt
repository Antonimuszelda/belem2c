# backend/requirements.txt - VERSÃO FINAL (GEE Real, SEM IA)

fastapi==0.111.0
uvicorn==0.30.1
gunicorn==22.0.0

pydantic==1.10.13

# Dependências geoespaciais
fiona==1.9.6
shapely==2.0.4
geojson==3.1.0

# Dependências GEE/STAC + Autenticação Google
earthengine-api==0.1.419
google-api-python-client
google-auth
google-auth-httplib2
# pystac-client==0.7.2 # Removido se não usar STAC diretamente
python-dotenv==1.0.1

# REMOVIDOS: openai, httpx, transformers, torch, accelerate